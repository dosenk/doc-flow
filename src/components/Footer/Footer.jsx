import React from 'react';
import { Box } from '@mui/material';

const Footer = () => {
  return (
    <footer>
      <Box style={{ height: '30px', background: 'gray' }} />
    </footer>
  );
};

Footer.propTypes = {};

export default Footer;
